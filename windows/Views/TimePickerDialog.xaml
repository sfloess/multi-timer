<Window x:Class="MultiTimer.Views.TimePickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Set Duration" Height="280" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Duration" FontSize="18" FontWeight="SemiBold" 
                   Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="Hours" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBox x:Name="HoursBox" Text="0" FontSize="24" TextAlignment="Center"
                         Background="{DynamicResource CardBrush}" Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource PrimaryBrush}" Padding="5"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <Button Content="▲" Width="30" Click="HoursUp_Click" Margin="2"/>
                    <Button Content="▼" Width="30" Click="HoursDown_Click" Margin="2"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Minutes" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBox x:Name="MinutesBox" Text="0" FontSize="24" TextAlignment="Center"
                         Background="{DynamicResource CardBrush}" Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource PrimaryBrush}" Padding="5"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <Button Content="▲" Width="30" Click="MinutesUp_Click" Margin="2"/>
                    <Button Content="▼" Width="30" Click="MinutesDown_Click" Margin="2"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="5">
                <TextBlock Text="Seconds" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBox x:Name="SecondsBox" Text="0" FontSize="24" TextAlignment="Center"
                         Background="{DynamicResource CardBrush}" Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource PrimaryBrush}" Padding="5"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <Button Content="▲" Width="30" Click="SecondsUp_Click" Margin="2"/>
                    <Button Content="▼" Width="30" Click="SecondsDown_Click" Margin="2"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Padding="15,5" Margin="0,0,10,0" MinWidth="80"/>
            <Button Content="OK" Click="Ok_Click" Padding="15,5" MinWidth="80" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
