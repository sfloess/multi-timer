<UserControl x:Class="MultiTimer.Views.TimerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MultiTimer.ViewModels"
             Background="{DynamicResource CardBackgroundBrush}"
             BorderBrush="{DynamicResource BorderBrush}"
             BorderThickness="1"
             Margin="0,0,0,10">

    <Border CornerRadius="8" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,8">
                <CheckBox DockPanel.Dock="Left"
                          IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=LostFocus}"
                         FontWeight="SemiBold" FontSize="14"
                         BorderThickness="0" Background="Transparent"/>
            </DockPanel>

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding DisplayTime}"
                           FontSize="32" FontFamily="Consolas" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryBrush}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="2,0">
                        <TextBlock Text="H" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"/>
                        <TextBox Text="{Binding Hours, UpdateSourceTrigger=LostFocus}" Width="35" TextAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text=":" VerticalAlignment="Center" Margin="2,5,2,0" Foreground="{DynamicResource TextMutedBrush}"/>
                    <StackPanel Margin="2,0">
                        <TextBlock Text="M" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"/>
                        <TextBox Text="{Binding Minutes, UpdateSourceTrigger=LostFocus}" Width="35" TextAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text=":" VerticalAlignment="Center" Margin="2,5,2,0" Foreground="{DynamicResource TextMutedBrush}"/>
                    <StackPanel Margin="2,0">
                        <TextBlock Text="S" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"/>
                        <TextBox Text="{Binding Seconds, UpdateSourceTrigger=LostFocus}" Width="35" TextAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Start" Command="{Binding StartCommand}"
                        Width="70" Margin="0,0,5,0" Padding="5,4"
                        Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderBrush="Transparent"/>
                <Button Content="Pause" Command="{Binding PauseCommand}"
                        Width="70" Margin="0,0,5,0" Padding="5,4"/>
                <Button Content="Reset" Command="{Binding ResetCommand}"
                        Width="60" Margin="0,0,5,0" Padding="5,4"/>
            </StackPanel>

            <StackPanel Grid.Row="3">
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=LostFocus}"
                         AcceptsReturn="True" Height="40" TextWrapping="Wrap"
                         FontSize="12" ToolTip="Notes"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
