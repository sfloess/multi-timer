<Window x:Class="MultiTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MultiTimer.ViewModels"
        xmlns:controls="clr-namespace:MultiTimer.Controls"
        mc:Ignorable="d"
        Title="Multi-Timer" Height="600" Width="450"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">

    <DockPanel Margin="10">
        <!-- Toolbar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Timer" Command="{Binding AddTimerCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Move Up" Command="{Binding MoveUpCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Move Down" Command="{Binding MoveDownCommand}" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Clear All" Command="{Binding ClearAllCommand}" Padding="10,5"/>
        </StackPanel>

        <!-- Timer List -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Timers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBrush}" 
                                CornerRadius="8" 
                                Margin="0,0,0,10" 
                                Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="SpaceBetween">
                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                             Background="Transparent" BorderThickness="0" Foreground="White" FontWeight="Bold"/>
                                    <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                                </StackPanel>

                                <TextBlock Grid.Row="1" Text="{Binding DisplayTime}" 
                                           FontSize="32" HorizontalAlignment="Center" Margin="0,10"/>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Start/Pause" Command="{Binding StartStopCommand}" Margin="0,0,5,0"/>
                                    <Button Content="Reset" Command="{Binding ResetCommand}" Margin="0,0,5,0"/>
                                    <Button Content="Edit" Command="{Binding EditCommand}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
